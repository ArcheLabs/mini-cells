[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "mini-cells"
version = "0.1.0"
description = "MiniCells cellular language model research and CLM-0.1 Research Preview"
requires-python = ">=3.11"
dependencies = ["torch", "numpy", "pyyaml", "matplotlib", "pandas", "tokenizers>=0.15"]

[project.scripts]
minicells-clm = "minicells.clm_cli:main"

[project.optional-dependencies]
dev = ["pytest", "ruff"]
lm = ["datasets>=2.18"]

[tool.setuptools]
package-dir = {"" = "research"}

[tool.setuptools.packages.find]
where = ["research"]

[tool.pytest.ini_options]
pythonpath = ["research"]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"
