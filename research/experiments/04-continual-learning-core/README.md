# Continual-Learning Core Experiments

Each Core Validation directory is an experiment adapter. It links the frozen protocol/report and imports implementation from the reusable `minicells` package. Scientific decisions remain in canonical artifacts.
