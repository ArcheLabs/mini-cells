# Functional Boundary Oracle 001 — Kaggle launch interruption, seed 26090511

## Classification

**Infrastructure interruption. Not a scientific PASS or FAIL.**

The first formal attempt for seed `26090511` exited with code 1 while the Granite checkpoint was still being materialized / before any formal result artifact was emitted. No `artifacts/experiments/functional-boundary-oracle-001/seed-26090511/result.json` was published.

Observed launcher tail:

```text
[seed=26090511] running formal GPU experiment
Fetching 10 files: 100% ...
Loading weights: 40% ... Materializing param=model.layers.9.self_attn.k_proj.weight
...
CalledProcessError ... run_formal_seed.py ... returned non-zero exit status 1
```

Because the previous Mutation 001 seed2 had stalled in the same hosted-GPU workflow, stale GPU/process ownership is a leading infrastructure hypothesis, but it is **not treated as confirmed root cause** without the child traceback / GPU process diagnostics.

## Remediation

No frozen scientific protocol, prompt split, score, optimizer rule, threshold, seed, or gate was changed.

Engineering-only remediation adds:

- Kaggle GPU free-memory preflight (minimum 12,000 MiB before each formal seed);
- listing of active GPU compute processes and matching research subprocesses;
- streaming child stdout/stderr to a durable launcher log;
- automatic `nvidia-smi` and last-120-lines dump after child failure.

A clean Kaggle runtime/session should be used for the rerun. If the preflight reports insufficient free GPU memory, restart the runtime rather than killing an unknown process.
