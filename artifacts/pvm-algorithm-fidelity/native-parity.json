{
  "schema": "minicells.native-training-parity.v1",
  "status": "BLOCKED_PYTORCH_UNAVAILABLE",
  "tolerance": {"absolute": 0.0005, "relative": 0.0005},
  "steps_required": [1, 2, 4, 16],
  "reason": "tools/export_training_fidelity.py imports the production research path and fails closed because torch is unavailable"
}
