[package]
name = "minicells-training-service"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
crate-type = ["cdylib"]

[features]
production = []
tree = []
tree_leaf_only = ["tree"]
tree_reducer_only = ["tree"]
tree_finalizer_only = ["tree"]

[dependencies]
minicells-training-ref = { path = "../minicells-training-ref" }
